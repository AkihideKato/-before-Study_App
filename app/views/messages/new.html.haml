.contents.row
  .container
    = form_with(model: @message, local: true) do |form|
      %h3
        = "投稿"
        .container-form
          .container-form-title
            = form.text_field :title, placeholder: "title"
          .container-form-text
            = form.text_area :text, placeholder: "text" , rows: "10",'data-provider': :summernote
            .button
              %a.bnt-massage
                = form.submit "送信"
                %span 押してください.